# Tag Atlas

Sign in, write up your projects with tags (or upload a `.md`/`.txt` file), then
click tag bubbles to filter and rank them. See `SETUP.md` for the full,
click-by-click setup guide — start there.
